# Archived Workflows

`shipment_csv_app_v1_9_with_havn_replacement.py` preserves the complete v1.9 application before the Havn Returns and Outbound Replacement workflows were retired from the active app.

This file is intentionally not imported by `shipment_csv_app.py`, so PyInstaller does not include it in the Windows executable. Restore individual sections from this snapshot if either workflow is needed again.
